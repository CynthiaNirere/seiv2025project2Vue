VITE_API_URL=http://localhost:3000

# Development environment variables for the frontend only.
# Do NOT put JavaScript here. Vite loads only key=value lines.
# VITE_API_URL used by the frontend (no auth layer now).
