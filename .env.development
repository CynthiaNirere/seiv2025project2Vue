VITE_API_URL=http://localhost:3000

# Development environment variables for the frontend
# - VITE_API_URL: base URL for backend API during `npm run dev`

export default defineConfig({
  plugins: [vue()],
  server: {
    proxy: {
      '/auth': {
        target: 'http://localhost:3000',
        changeOrigin: true,
        rewrite: (path) => path, // keep /auth/...
      },
      '/courses': { target: 'http://localhost:3000', changeOrigin: true },
    },
  },
});
